name: Test ABME
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Get the repository code
      uses: actions/checkout@v3    #Используем action checkout из готового маркект плейса
    - name: Install dependecies
      run: npm ci
    - name: Test application
      run: npm run test
      

